//viết hamf giới thiệu ban banr thân
// nhận vào thông tin tên va khóa học, sau đó output ra console

let name = "loc"; 
let course = "javascript";
function greeting(name, course){// tham số - par
document.write(`my name is ${name}. my course is ${ course}`);
}

greeting (name, course);