/**
 * Viết hàm findPrimes() nhận vào hai số nStart, nEnd và in ra tất cả số nguyên tố trong khoảng [nStart, nEnd]
 * Nếu có, trả về true. Ngược lại, trả về false.
 * 
 * Ví dụ: 
 * ifindPrimes(2, 24)
 * 
 * Output:
 * 2
 * 3
 * 5
 * 7
 * 11
 * 13
 * 17
 * 19
 * 23
 *  
 * */ 

function findPrimes(nStart, nEnd) {
    for(let i=nStart; i=nEnd;i++){
      let array =findPrimes[i]
      if(array%i==0)
                break;

            }
            let result=[];
           

            if(array%i>0){ result.push(array[i]);

            }

            console.log(result[i]);
            }
        
    
    ///bai này e khong biết lam
    


findPrimes(2, 24);